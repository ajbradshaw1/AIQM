[pytest]
# Offline unit tests live in tests/ and the self-contained post-processing tool.
# scripts/ holds hardware probes, so automated collection stays out of it.
testpaths = tests tools/rheed_postprocessing_labeling/tests
