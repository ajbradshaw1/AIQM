[pytest]
# Offline unit tests live in tests/ and the self-contained post-processing
# package. scripts/ holds hardware probes that may open instrument links, so
# automated collection must stay out of that directory.
testpaths =
    tests
    tools/rheed_postprocessing_labeling/tests
